name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: make
      working-directory: ./src
      run: make gtest
    - name: gtest
      working-directory: ./src
      run: ./gtest
