name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - working-directory: ./gatsby
    - uses: actions/checkout@v2
    - name: make
      run: make gtest
    - name: gtest
      run: ./gtest
